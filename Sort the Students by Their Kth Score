class Solution(object):
    def sortTheStudents(self, score, k):
        """
        :type score: List[List[int]]
        :type k: int
        :rtype: List[List[int]]
        """
        score=sorted(score, key=lambda a: a[k])
        score.reverse()
        return score
        
